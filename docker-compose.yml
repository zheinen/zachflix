services:
  db:
    image: postgres:17
    container_name: zachflix-db
    environment:
      POSTGRES_DB: zachflix
      POSTGRES_USER: zachflix
      POSTGRES_PASSWORD: zachflix_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
